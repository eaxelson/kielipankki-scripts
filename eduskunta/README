
For eduskunta vrt file INPUT.vrt, run:

  cat INPUT.vrt | preprocess-vrt.pl | add-paragraph-attributes.pl | postprocess-vrt.pl > OUTPUT.vrt

Fix warnings generated by add-paragraph-attributes.pl and run the above again.
Check that the values of the appended attributes seem reasonable:

  get-attribute-stats.sh OUTPUT.vrt

Then run korp-make for OUTPUT.vrt.
